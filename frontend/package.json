{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prod": "tsc",
    "dev": "tsc && vite build && vite serve --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "host": "tsc && vite build --host"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.2"
  },
  "dependencies": {
    "typescript": "^5.9.3",
    "socket.io-client": "^4.8.1"
  }
}

